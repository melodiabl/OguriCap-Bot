# =========================================================
<<<<<<< HEAD
# GLOBAL SSL (UNA SOLA VEZ)
# =========================================================
ssl_session_cache shared:SSL:20m;
ssl_session_timeout 10m;
ssl_session_tickets off;

# =========================================================
# RATE LIMIT ZONES
=======
# HTTP -> HTTPS (DOMINIOS)
# =========================================================
server {
  listen 80;
  server_name melodiaauris.qzz.io oguricap.ooguy.com;

  # Let's Encrypt
  location /.well-known/acme-challenge/ {
    root /var/www/certbot;
  }

  location / {
    return 301 https://$host$request_uri;
  }
}

# =========================================================
# RATE LIMITING ZONES
>>>>>>> 4f37e52130327d4550d0ae49bfd68dbd08db8a62
# =========================================================
limit_req_zone $binary_remote_addr zone=api:10m rate=100r/s;
limit_req_zone $binary_remote_addr zone=login:10m rate=10r/m;
limit_req_zone $binary_remote_addr zone=general:10m rate=200r/s;

# =========================================================
# WEBSOCKET MAP
# =========================================================
map $http_upgrade $connection_upgrade {
  default upgrade;
  '' close;
}

# =========================================================
<<<<<<< HEAD
# HTTP -> HTTPS (OGURI)
# =========================================================
server {
  listen 80;
  server_name oguricap.ooguy.com;

  location /.well-known/acme-challenge/ {
    root /var/www/certbot;
  }

  location / {
    return 301 https://$host$request_uri;
  }
}

# =========================================================
# HTTP -> HTTPS (MELODIA)
# =========================================================
server {
  listen 80;
  server_name melodiaauris.qzz.io;

  location /.well-known/acme-challenge/ {
    root /var/www/certbot;
  }

  location / {
    return 301 https://$host$request_uri;
  }
}

# =========================================================
# HTTPS - OGURI
# =========================================================
server {
  listen 443 ssl http2;
  server_name oguricap.ooguy.com;

  ssl_certificate /etc/letsencrypt/live/oguricap.ooguy.com/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/oguricap.ooguy.com/privkey.pem;
=======
# HTTPS (DOMINIOS)
# =========================================================
server {
  listen 443 ssl;
  server_name melodiaauris.qzz.io oguricap.ooguy.com;

  # ================= SSL =================
  ssl_certificate /etc/letsencrypt/live/melodiaauris.qzz.io/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/melodiaauris.qzz.io/privkey.pem;
>>>>>>> 4f37e52130327d4550d0ae49bfd68dbd08db8a62

  ssl_protocols TLSv1.2 TLSv1.3;
  ssl_prefer_server_ciphers off;

  # ================= SECURITY =================
  add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;
  add_header X-Frame-Options "SAMEORIGIN" always;
  add_header X-Content-Type-Options "nosniff" always;

  server_tokens off;
  client_max_body_size 50M;

<<<<<<< HEAD
  # ================= AUTH =================
  location ~ ^/api/auth/(login|register|reset-password) {
    limit_req zone=login burst=3 nodelay;
=======
  access_log /var/log/nginx/access.log;
  error_log /var/log/nginx/error.log warn;

  # =====================================================
  # AUTH ROUTES
  # =====================================================
  location ~ ^/api/auth/(login|register|reset-password) {
    limit_req zone=login burst=3 nodelay;

>>>>>>> 4f37e52130327d4550d0ae49bfd68dbd08db8a62
    proxy_pass http://127.0.0.1:3001;
    proxy_http_version 1.1;

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;
  }

<<<<<<< HEAD
  # ================= PANEL =================
  location / {
    limit_req zone=general burst=200 nodelay;
=======
  # =====================================================
  # API
  # =====================================================
  location /api/ {
    limit_req zone=api burst=100 nodelay;

    proxy_pass http://127.0.0.1:3001;
    proxy_http_version 1.1;

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;

    proxy_connect_timeout 120s;
    proxy_send_timeout 120s;
    proxy_read_timeout 120s;

    proxy_buffering off;
  }

  # =====================================================
  # SOCKET.IO (CRÍTICO)
  # =====================================================
  location /socket.io/ {
    proxy_pass http://127.0.0.1:3001;
    proxy_http_version 1.1;

    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;

    proxy_buffering off;
    proxy_read_timeout 86400;
  }

  # =====================================================
  # PANEL (Next.js)
  # =====================================================
  location / {
    limit_req zone=general burst=200 nodelay;

>>>>>>> 4f37e52130327d4550d0ae49bfd68dbd08db8a62
    proxy_pass http://127.0.0.1:3000;
    proxy_http_version 1.1;

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;
    add_header Cache-Control "no-store" always;
    proxy_buffering off;
  }

<<<<<<< HEAD
  # ================= API =================
  location /api/ {
    limit_req zone=api burst=100 nodelay;
    proxy_pass http://127.0.0.1:3001;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;
  }

  # ================= WEBSOCKET =================
  location /socket.io/ {
    proxy_pass http://127.0.0.1:3001;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_buffering off;
  }

=======
  # =====================================================
  # STATIC
  # =====================================================
  location /_next/static/ {
    proxy_pass http://127.0.0.1:3000;
    expires 1y;
    add_header Cache-Control "public, immutable";
  }

  location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
    proxy_pass http://127.0.0.1:3000;
    expires 1y;
    add_header Cache-Control "public, immutable";
  }

  # =====================================================
  # HEALTH
  # =====================================================
>>>>>>> 4f37e52130327d4550d0ae49bfd68dbd08db8a62
  location /health {
    return 200 "healthy\n";
  }
}
<<<<<<< HEAD

# =========================================================
# HTTPS - MELODIA
# =========================================================
server {
  listen 443 ssl http2;
  server_name melodiaauris.qzz.io;

  ssl_certificate /etc/letsencrypt/live/oguricap.ooguy.com/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/oguricap.ooguy.com/privkey.pem;

  ssl_protocols TLSv1.2 TLSv1.3;
  ssl_prefer_server_ciphers off;

  add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;
  add_header X-Frame-Options "SAMEORIGIN" always;
  add_header X-Content-Type-Options "nosniff" always;

  server_tokens off;
  client_max_body_size 50M;

  location / {
    proxy_pass http://127.0.0.1:3000;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;
    add_header Cache-Control "no-store" always;
    proxy_buffering off;
  }

  location /api/ {
    proxy_pass http://127.0.0.1:3001;
  }

  location /socket.io/ {
    proxy_pass http://127.0.0.1:3001;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
  }

  location /health {
    return 200 "healthy\n";
  }
}
=======
>>>>>>> 4f37e52130327d4550d0ae49bfd68dbd08db8a62
